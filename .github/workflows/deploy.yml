name: Deploy to Dokploy

on:
  push:
    branches: [main]

jobs:
  identify-and-deploy:
    runs-on: ubuntu-latest
    outputs:
      apps: ${{ steps.set-apps.outputs.apps }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0 # Necessary for Turbo to compare commits

      - name: Install Bun
        uses: oven-sh/setup-bun@v2

      - name: Install Dependencies
        run: bun install

      - id: set-apps
        name: Identify Changed Apps
        # This filters apps that changed relative to the previous commit
        run: |
          APPS=$(bun x turbo build --filter='[HEAD^1]' --dry-run=json | jq -c '.packages | map(select(test("^//$|^@") | not))')
          echo "Identified apps to deploy: $APPS"
          echo "apps=$APPS" >> $GITHUB_OUTPUT

  build-and-push:
    needs: identify-and-deploy
    if: ${{ needs.identify-and-deploy.outputs.apps != '[]' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        app_name: ${{ fromJson(needs.identify-and-deploy.outputs.apps) }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: San-Diego-Fine-Woodworkers-Association
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase Owner Name
        run: |
          echo "OWNER_LC=${OWNER,,}" >>${GITHUB_ENV}
        env:
          OWNER: '${{ github.repository_owner }}'

      - name: Get Application Version
        id: get_version
        run: |
          VERSION=$(jq -r '.version' apps/${{ matrix.app_name }}/package.json)
          echo "VERSION=$VERSION" >>${GITHUB_ENV}

      - name: Create Docker Image Metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ env.OWNER_LC }}/${{ matrix.app_name }}
          tags: |
            type=raw,value=latest
            type=sha,format=short
            type=raw,value=${{ env.VERSION }}
          labels: |
            org.opencontainers.image.title=${{ matrix.app_name }}
            org.opencontainers.image.version=${{ env.VERSION }}
            org.opencontainers.image.source=https://github.com/${{ github.repository }}

      - name: Determine Dockerfile
        id: dockerfile
        run: |
          # Per-app override wins; otherwise pick the shared db-aware image when the
          # app ships drizzle migrations, else the plain shared image.
          if [ -f "apps/${{ matrix.app_name }}/Dockerfile.prod" ]; then
            echo "path=apps/${{ matrix.app_name }}/Dockerfile.prod" >> $GITHUB_OUTPUT
          elif [ -f "apps/${{ matrix.app_name }}/drizzle.config.ts" ]; then
            echo "path=Dockerfile.app.db.prod" >> $GITHUB_OUTPUT
          else
            echo "path=Dockerfile.app.prod" >> $GITHUB_OUTPUT
          fi

      - name: Build and Push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ${{ steps.dockerfile.outputs.path }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            APP_NAME=${{ matrix.app_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Get Application ID Secret Name
        run: |
          CLEAN_NAME=$(echo "${{ matrix.app_name }}" | tr '-' '_' | tr '[:lower:]' '[:upper:]')
          APPLICATION_ID="DOKPLOY_APPLICATION_ID_$CLEAN_NAME"
          echo "APPLICATION_ID=$APPLICATION_ID" >>${GITHUB_ENV}

      - name: Deploy to Dokploy
        run: |
          curl --fail-with-body -L -X 'POST' \
            '${{ secrets.DOKPLOY_HOST }}/api/application.deploy' \
            -H 'accept: application/json' \
            -H 'x-api-key: ${{ secrets.DOKPLOY_AUTH_TOKEN }}' \
            -H 'Content-Type: application/json' \
            -d '{
              "applicationId": "${{ secrets[env.APPLICATION_ID] }}"
            }'